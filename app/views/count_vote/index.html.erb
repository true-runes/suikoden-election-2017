<div class="container">
  <div class="page-header" id="banner">
    <div class="row">
      <div class="col-lg-12">
        <h1><strong>開票ツイート</strong></h1>
        <div class="bs-component">
          <%= paginate @kaminaried_tweets, window: 6 %>
          <% @tweets = @kaminaried_tweets %>
          <%= render 'show_tweets_panel_with_image' %>
        </div>
        <%= paginate @kaminaried_tweets, window: 6 %>
      </div>
    </div>
  </div>
</div>
