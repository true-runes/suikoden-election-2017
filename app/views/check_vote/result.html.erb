<div class="container">
  <div class="page-header" id="banner">
    <div class="row">
      <div class="col-lg-12">
        <h1><strong>投票チェック結果</strong></h1>
        <h4>※現在は削除されたツイートも表示されます</h4>
        <%= "<strong>チェック結果は 0 件でした。</strong>".html_safe if @result_tweets.empty? %>
        <% @tweets = @result_tweets %>
        <%= render 'show_tweets_panel' %>
      </div>
    </div>
  </div>
</div>
